import { ProgressWithDirective } from './progress-with.directive';

describe('ProgressWithDirective', () => {
  it('should create an instance', () => {
    const directive = new ProgressWithDirective();
    expect(directive).toBeTruthy();
  });
});
